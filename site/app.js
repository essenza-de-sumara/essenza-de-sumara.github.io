// Configure o WhatsApp da loja com código do país + DDD + número, apenas dígitos.
const WHATSAPP_NUMBER = '5562999883730';
const perfumes = [
{id:1,name:'Khamrah',brand:'LATTAFA',gender:'Unissex',family:'Âmbar · Especiarias · Baunilha',price:289.90,image:'khamrah.webp',badge:'INTENSO & ENVOLVENTE',description:'Uma fragrância de presença marcante para quem aprecia acordes quentes e adocicados.',notes:'Quente, especiado e adocicado'},
{id:2,name:'Yara',brand:'LATTAFA',gender:'Feminino',family:'Floral · Frutado · Cremoso',price:219.90,image:'yara.webp',badge:'DELICADO & CATIVANTE',description:'Uma proposta delicada e envolvente, com uma personalidade floral e um toque doce.',notes:'Floral, frutado e cremoso'},
{id:3,name:'Asad',brand:'LATTAFA',gender:'Masculino',family:'Especiarias · Madeiras · Âmbar',price:249.90,image:'asad.webp',badge:'FORTE & MARCANTE',description:'Um perfume de caráter intenso para quem gosta de uma assinatura amadeirada e especiada.',notes:'Amadeirado, quente e especiado'},
{id:4,name:'Yara Moi',brand:'LATTAFA',gender:'Feminino',family:'Floral · Amadeirado · Adocicado',price:239.90,image:'moi.webp',badge:'SUAVE & SOFISTICADO',description:'Uma combinação de delicadeza floral e notas envolventes para acompanhar momentos especiais.',notes:'Floral, amadeirado e adocicado'}];
let filter='Todos';const $=s=>document.querySelector(s);const money=n=>n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});const normalized=s=>s.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();
function render(){const query=normalized($('#search').value);let list=perfumes.filter(p=>(filter==='Todos'||p.gender===filter)&&normalized([p.name,p.brand,p.family].join(' ')).includes(query));const sort=$('#sort').value;if(sort==='asc')list.sort((a,b)=>a.price-b.price);if(sort==='desc')list.sort((a,b)=>b.price-a.price);if(sort==='name')list.sort((a,b)=>a.name.localeCompare(b.name));$('#count').textContent=`${list.length} fragrância${list.length===1?'':'s'}`;$('#empty').hidden=list.length>0;$('#products').innerHTML=list.map(p=>`<article class="product"><button class="product-image" data-id="${p.id}" aria-label="Ver detalhes de ${p.name}"><span class="badge">${p.badge}</span><img src="${p.image}" alt="Frasco do perfume ${p.name} da Lattafa" loading="lazy"></button><div class="product-meta"><span>${p.brand}</span><span>${p.gender.toUpperCase()} · 100 ML</span></div><h3>${p.name}</h3><p class="notes">${p.family}</p><div class="price">${money(p.price)}<small>preço ilustrativo</small></div><button class="details-button" data-id="${p.id}">Conhecer fragrância <span>↗</span></button></article>`).join('');}
$('#filters').addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;filter=b.dataset.filter;$('#filters').querySelectorAll('button').forEach(x=>{x.classList.toggle('active',x===b);x.setAttribute('aria-pressed',x===b)});render()});$('#search').addEventListener('input',render);$('#sort').addEventListener('change',render);$('#reset').onclick=()=>{$('#search').value='';$('#filters button').click()};
function showProduct(id){const p=perfumes.find(p=>p.id===id);if(!p)return;$('#detail-content').innerHTML=`<img src="${p.image}" alt="Frasco ${p.name}"><div><p class="eyebrow">${p.brand} · ${p.gender.toUpperCase()}</p><h2>${p.name}</h2><p>Eau de Parfum · 100 ml</p><p>${p.description}</p><div class="note-label">PERFIL OLFATIVO</div><p>${p.notes}</p><strong>${money(p.price)}</strong><p>Valor ilustrativo. Consulte preço e disponibilidade com a loja.</p><button class="gold-button" id="interest">Tenho interesse ↗</button></div>`;$('#detail').showModal();$('#interest').onclick=()=>{$('#detail').close();contact(p.name)}}
$('#products').onclick=e=>{const b=e.target.closest('[data-id]');if(b)showProduct(Number(b.dataset.id))};let message='';function contact(name){message=name?`Olá, Essenza de Sumara! Tenho interesse no perfume ${name}. Poderia informar o preço e a disponibilidade?`:'Olá, Essenza de Sumara! Gostaria de ajuda para escolher um perfume árabe.';if(/^\d{10,15}$/.test(WHATSAPP_NUMBER)){window.open(`https://wa.me/${WHATSAPP_NUMBER}?text=${encodeURIComponent(message)}`,'_blank','noopener,noreferrer');return}$('#contact-text').textContent=message;$('#copy-status').textContent='';$('#contact').showModal()};document.querySelectorAll('.contact').forEach(b=>b.onclick=()=>contact());document.querySelectorAll('dialog').forEach(d=>{d.querySelector('.close').onclick=()=>d.close();d.addEventListener('click',e=>{if(e.target===d){const r=d.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)d.close()}})});$('#copy').onclick=async()=>{try{await navigator.clipboard.writeText(message);$('#copy-status').textContent='Mensagem copiada!'}catch{$('#copy-status').textContent='Selecione e copie a mensagem acima.'}};render();
if(document.modelContext?.registerTool){try{Promise.resolve(document.modelContext.registerTool({name:'search_perfume_catalog',description:'Busca perfumes e atualiza os resultados visíveis do catálogo.',inputSchema:{type:'object',properties:{query:{type:'string'}},required:['query'],additionalProperties:false},annotations:{readOnlyHint:false},execute(input){if(!input||typeof input.query!=='string')throw new Error('query deve ser texto');$('#search').value=input.query;filter='Todos';$('#filters button').click();render();return {query:input.query,count:$('#count').textContent}}})).catch(()=>{})}catch{}}
